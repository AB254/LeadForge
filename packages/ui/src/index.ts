/**
 * @leadforge/ui
 *
 * Shared UI component library (placeholder).
 *
 * All production components currently live in `apps/web/src/components`.
 * This package exists so that shared components can be extracted here
 * incrementally without restructuring imports across the monorepo.
 *
 * Re-export nothing for now — consumers should import directly from
 * `apps/web/src/components` until components are migrated here.
 */

export {};
